# MySQL Database Configuration for Testing
DB_USER=testuser                     # MySQL username for testing
DB_PASSWORD=testpassword             # MySQL password for testing
DB_NAME=fivver_doup_test             # Test database name
DB_HOST=127.0.0.1                    # Test MySQL host (localhost)
DB_PORT=3306                          # Test MySQL port (default: 3306)

# Sequelize Test Configuration
TEST_DB_DIALECT=mysql                # MySQL dialect for Sequelize

# JWT Secret for Testing
JWT_SECRET=your_jwt_secret           # JWT secret for testing (replace with actual secret)

# Optional: You can set other environment variables for testing purposes if needed
