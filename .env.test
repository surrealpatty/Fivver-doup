# Database Configuration for Testing
DB_USER=root                             # Database username for testing
DB_PASSWORD=f0^:8t1#qaC7                 # Database password for testing (ensure no extra spaces)
DB_NAME=fivver_doup_test                  # Test database name (different from development)
DB_HOST=localhost                         # Database host for testing
DB_PORT=3306                              # Default MySQL port
DB_DIALECT=mysql                          # Sequelize dialect for MySQL

# JWT Secret for Signing Tokens
JWT_SECRET=your_jwt_secret               # Replace with a secure, random secret key
JWT_EXPIRATION=1h                        # Token expiration time (example: 1 hour)

# Port Configuration
PORT=3001                                 # Port for running tests (to avoid conflicts with the dev server)
NODE_ENV=test                             # Environment for testing

# File Uploads or Caching Settings
UPLOAD_DIR=./uploads                      # Directory for storing uploaded files (ensure this path exists)
