TEST_DB_USER=testuser                   # MySQL username for testing
TEST_DB_PASSWORD=testpassword           # MySQL password for testing
TEST_DB_NAME=fivver_doup_test           # Test database name
TEST_DB_HOST=127.0.0.1                  # Test MySQL host (using 127.0.0.1 for localhost)
TEST_DB_PORT=3306                       # Test MySQL port (default is 3306)
TEST_DB_DIALECT=mysql                   # MySQL dialect for Sequelize in the test environment
JWT_SECRET=your_jwt_secret              # JWT secret for testing (replace with actual secret for testing)
