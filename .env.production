# Database Configuration for Production
DB_USER=prod_user                         # Production DB username
DB_PASSWORD=prod_secure_password          # Production DB password (make sure it's a strong password)
DB_NAME=fivver_doup_prod                  # Production DB name
DB_HOST=prod-db-host                      # Production DB host
DB_PORT=3306                               # Default MySQL port
DB_DIALECT=mysql                           # Sequelize dialect for MySQL

# JWT Secret for Signing Tokens (make sure to use a strong, secret key)
JWT_SECRET=prod_jwt_secret                # Secure production JWT secret key
JWT_EXPIRATION=1h                         # Token expiration time (e.g., 1 hour)

# Port Configuration
PORT=8000                                 # Port for running the app in production
NODE_ENV=production                       # Environment: 'development', 'production', or 'test'

# File Uploads or Caching Settings
UPLOAD_DIR=/var/www/uploads               # Use an appropriate path for production file uploads
