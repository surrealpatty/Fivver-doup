module.exports = {
  presets: [
    // Ensures Vue CLI's built-in preset is applied for compatibility with modern JavaScript features
    '@vue/cli-plugin-babel/preset'
  ],
  // Uncomment and customize the following section if additional plugins or options are needed:
  // plugins: [
  //   // Add specific Babel plugins if needed
  // ]
};

