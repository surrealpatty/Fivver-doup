// src/api.d.ts

declare module 'api' {
  interface SomeApiResponse {
    data: string; // Replace 'any' with a more specific type like 'string'
  }
  let variable: string; // Replace 'any' with a specific type, e.g., 'string'
}
