// src/test/auth.test.d.ts

// Ensure Jest types are available in this test file by referencing Jest's type definitions.
/// <reference types="jest" />

// Declare global types if needed for your tests, but do not put any executable code (like describe/it).
declare global {
    // Example: if you needed to augment some global module or type, you could do it here
    // declare module 'your-module' {
    //   export function yourFunction(): void;
    // }
  }
  
  export {}; // Ensure it's an empty module to satisfy TypeScript's module system
  