// src/test/sample.test.d.ts

// Import the mathLib module (replace with your actual module and path if needed)
import * as mathLib from 'math-lib';

// Declare the sum function with more specific types
declare function sum(a: unknown, b: unknown): unknown; // Use unknown instead of any
