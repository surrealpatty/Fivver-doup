// src/test/example.test.ts
import { something } from '../index'; // Correct relative path

describe('Example Test', () => {
  it('should work correctly', () => {
    expect(something).toBeDefined();
  });
});
