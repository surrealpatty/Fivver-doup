// src/types/jwt.d.ts

export interface JwtPayload {
    userId: string; // Or whatever fields your JWT contains
    email: string;  // Adjust based on your actual JWT structure
    // Add other properties as needed
  }
  