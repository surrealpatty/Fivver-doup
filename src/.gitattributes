# Automatically normalize line endings for text files
* text=auto

# Handle binary files with Git LFS (Large File Storage)
docs/mysql-9.1.0-winx64.msi filter=lfs diff=lfs merge=lfs -text
*.exe filter=lfs diff=lfs merge=lfs -text

# Example: Adding image files for Git LFS tracking
*.jpg filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.gif filter=lfs diff=lfs merge=lfs -text

# Example: Add other large files like zip, tar, etc. for LFS tracking
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar filter=lfs diff=lfs merge=lfs -text
