# Database Configuration for Testing
DB_USER=root                              # Replace with your test database username if different
DB_PASSWORD=your_test_db_password         # Replace with your test database password
DB_NAME=fivver_doup_test                  # Name of the test database
DB_HOST=localhost                         # Host for the test database
DB_PORT=3306                              # Default MySQL port

# JWT Secret for Testing
JWT_SECRET=test_jwt_secret                # Use a separate JWT secret for the test environment

# Environment and Port Configuration
NODE_ENV=test                             # Set the environment to 'test'
PORT=3001                                 # Use a different port for testing

# File Upload and Caching Settings
UPLOAD_DIR=./test_uploads                 # Directory for test-specific uploads
