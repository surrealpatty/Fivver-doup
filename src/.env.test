# Test Database Configuration
TEST_DB_USER=root                         # Replace with the username for your test database
TEST_DB_PASSWORD=your_test_db_password    # Replace with the password for your test database
TEST_DB_NAME=fivver_doup_test             # Name of the test database
TEST_DB_HOST=localhost                    # Host for the test database (default is localhost)
TEST_DB_PORT=3306                         # Default MySQL port for the test database

# JWT Secret for Testing
JWT_SECRET=test_jwt_secret                # Use a separate secret for testing

# Test-Specific Configuration
NODE_ENV=test                             # Set the environment to 'test'
PORT=3001                                 # Use a different port for testing to avoid conflicts

# File Uploads or Caching Settings for Tests
UPLOAD_DIR=./test_uploads                 # Directory for storing test-specific uploaded files (ensure this path exists)
