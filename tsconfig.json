{
  "compilerOptions": {
    "target": "ESNext",  // Set ECMAScript target version
    "module": "CommonJS",  // CommonJS module system for Node.js
    "strict": true,  // Enables strict type checking
    "esModuleInterop": true,  // Enables default imports for CommonJS modules
    "skipLibCheck": true,  // Skip type checking of declaration files (for faster compilation)
    "forceConsistentCasingInFileNames": true,  // Ensures consistent casing of file names
    "baseUrl": "./src",  // Base directory for resolving non-relative module imports
    "paths": {
      "@config/*": ["src/config/*"],  // Resolves '@config' to 'src/config/*'
      "@middlewares/*": ["src/middlewares/*"],  // Resolves '@middlewares' to 'src/middlewares/*'
      "@models/*": ["src/models/*"],  // Resolves '@models' to 'src/models/*'
      "@controllers/*": ["src/controllers/*"],  // Resolves '@controllers' to 'src/controllers/*'
      "@routes/*": ["src/routes/*"],  // Resolves '@routes' to 'src/routes/*'
      "@shared/*": ["src/shared/*"],  // Resolves '@shared' to 'src/shared/*'
      "@types/*": ["src/types/*"],  // Resolves '@types' to 'src/types/*'
      "*": ["node_modules/*", "src/*"]  // Resolves everything else from 'node_modules' and 'src/*'
    },
    "outDir": "./dist",  // Directory where compiled files will be placed
    "moduleResolution": "node",  // Resolves modules like Node.js would
    "allowJs": true,  // Allows JavaScript files in the project (optional)
    "resolveJsonModule": true,  // Allows importing JSON files as modules
    "experimentalDecorators": true,  // Allows use of decorators (required by Sequelize or other libraries)
    "emitDecoratorMetadata": true  // Emits metadata for decorators (required by Sequelize)
  },
  "include": [
    "src/**/*"  // Includes all files in the 'src' directory for type checking
  ],
  "exclude": [
    "node_modules"  // Excludes the 'node_modules' directory from type checking
  ]
}