{
  "compilerOptions": {
    "typeRoots": ["./node_modules/@types", "./src/types"],  // Add this line if not already included
    "types": ["node", "express", "jest"],                  // Ensure express is included in the types array
    "lib": ["ES2018", "DOM"],
    // other configurations...
  },
  "include": [
    "src/**/*",
    "**/*.test.ts"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
