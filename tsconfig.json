{
  "compilerOptions": {
    "target": "ESNext",  // Adjusts the JS output version to the latest ES standard
    "module": "CommonJS",  // For Node.js projects
    "strict": true,  // Enables all strict type-checking options
    "esModuleInterop": true,  // Ensures compatibility between CommonJS and ES modules
    "skipLibCheck": true,  // Skip type checking of declaration files for faster compilation
    "forceConsistentCasingInFileNames": true,  // Ensures consistent file name casing
    "baseUrl": "./src",  // The base directory for resolving modules (starting from src folder)
    "paths": {
      "@config/*": ["src/config/*"],  // Alias '@config' to 'src/config'
      "@middlewares/*": ["src/middlewares/*"],  // Alias '@middlewares' to 'src/middlewares'
      "@types/*": ["src/types/*"],  // Alias '@types' to 'src/types'
      "@models/*": ["src/models/*"],  // Alias '@models' to 'src/models'
      "@controllers/*": ["src/controllers/*"],  // Alias '@controllers' to 'src/controllers'
      "@routes/*": ["src/routes/*"],  // Alias '@routes' to 'src/routes'
      "@shared/*": ["src/shared/*"],  // Alias '@shared' to 'src/shared'
      "*": ["node_modules/*", "src/*"]  // Resolves all files in 'src' and 'node_modules'
    },
    "outDir": "./dist",  // The directory where compiled files will be output
    "moduleResolution": "node",  // Resolve modules like Node.js would
    "allowJs": true,  // Allow JavaScript files in the project (optional)
    "resolveJsonModule": true,  // Allow importing JSON files as modules
    "experimentalDecorators": true,  // Enables support for decorators (e.g., for Sequelize)
    "emitDecoratorMetadata": true  // Emits metadata for decorators (required by Sequelize)
  },
  "include": [
    "src/**/*"  // Includes all files in the 'src' directory for type checking
  ],
  "exclude": [
    "node_modules"  // Excludes the 'node_modules' directory from type checking
  ]
}
