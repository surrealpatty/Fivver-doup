{
  "compilerOptions": {
    "target": "ESNext",  // Set target ECMAScript version
    "module": "CommonJS",  // CommonJS module system for Node.js
    "strict": true,  // Enables strict type checking
    "esModuleInterop": true,  // Enables default imports for CommonJS modules
    "skipLibCheck": true,  // Skip type checking of declaration files (for faster compilation)
    "forceConsistentCasingInFileNames": true,  // Ensures consistent casing of file names
    "baseUrl": "./src", // or your base directory
    "paths": {
      "@types": ["types"],
      "@models/*": ["src/models/*"],  // Example for other aliases if needed
      "@controllers/*": ["src/controllers/*"]
    },
    "outDir": "./dist",  // Directory where the compiled files will be placed
    "moduleResolution": "node",  // Resolve modules like Node.js would
    "allowJs": true,  // Allows JavaScript files in the project (optional)
    "resolveJsonModule": true,  // Allows importing JSON files as modules
    "experimentalDecorators": true,  // Enable decorators
    "emitDecoratorMetadata": true,    // Emit metadata for decorators (required by some libraries like Sequelize)
  },
  "include": [
    "src/**/*"  // Includes all files in the 'src' directory for type checking
  ],
  "exclude": [
    "node_modules"  // Excludes the 'node_modules' directory from type checking
  ]
}
