# Database Configuration for Development
DB_USER=root
DB_PASSWORD="X^SE4Jzp$qfd1Fs2qfT*"
DB_NAME=fivver_doup_db
DB_HOST=localhost
DB_DIALECT=mysql

# JWT Secret for Signing Tokens (ensure this is strong and unique for production)
JWT_SECRET="mySuperSecretKey123"  # Change this in production to a secure, unpredictable value

# Port Configuration
PORT=3000
NODE_ENV=development  # Set environment: 'development', 'production', or 'test'

# Optional Production Database Configuration (Set these in production environment, not in development .env)
PROD_DB_USER="your_prod_db_user"
PROD_DB_PASSWORD="your_prod_db_password"
PROD_DB_NAME="your_prod_db_name"
PROD_DB_HOST="your_prod_db_host"

# Test Database Configuration (Optional: for testing environment)
TEST_DB_NAME=fivver_doup_test  # Specify a separate database for testing

# Optional Settings for File Uploads or Caching
UPLOAD_DIR="./uploads"  # Directory for storing uploaded files (adjust path if necessary)
CACHE_DIR="./cache"     # Directory for caching (if your app uses caching)

# Email Configuration (Optional: only include if needed for app notifications or registration)
MAIL_HOST="smtp.mailtrap.io"
MAIL_PORT=587
MAIL_USER="your_mailtrap_user"
MAIL_PASS="your_mailtrap_password"

# Debugging and Logging Configuration
LOG_LEVEL=debug  # Set to 'info' or 'error' for production
