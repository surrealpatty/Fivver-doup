# Database Configuration for Development
DB_USER=root
DB_PASSWORD="X^SE4Jzp$qfd1Fs2qfT*"
DB_NAME=fivver_doup_db
DB_HOST=localhost
DB_DIALECT=mysql

# JWT Secret for Signing Tokens
# Ensure this is strong and unique for production
JWT_SECRET="mySuperSecretKey123"  # Change this to a more secure key in production

# Port Configuration
PORT=3000
NODE_ENV=development  # Set environment: 'development', 'production', or 'test'

# Optional Production Database Configuration (These should be set only in the production environment)
PROD_DB_USER="your_prod_db_user"
PROD_DB_PASSWORD="your_prod_db_password"
PROD_DB_NAME="your_prod_db_name"
PROD_DB_HOST="your_prod_db_host"

# Test Database Configuration (Use a separate DB for testing to avoid conflicts)
TEST_DB_NAME=fivver_doup_test  # Separate database for testing environment

# Optional File Uploads or Caching Settings
UPLOAD_DIR="./uploads"  # Directory for storing uploaded files (ensure this path is correct for your system)
CACHE_DIR="./cache"     # Directory for caching (if used)

# Email Configuration (Optional: Only include if app requires email functionality)
MAIL_HOST="smtp.mailtrap.io"  # Example for Mailtrap, change to your actual email provider
MAIL_PORT=587
MAIL_USER="your_mailtrap_user"
MAIL_PASS="your_mailtrap_password"

# Debugging and Logging Configuration
LOG_LEVEL=debug  # Change to 'info' or 'error' for production

# Optional SSL and security settings for production
# Uncomment and configure if your database or application uses SSL or other secure connections
# DB_SSL=true  # Uncomment if your DB connection requires SSL (e.g., for cloud-hosted databases)
# NODE_TLS_REJECT_UNAUTHORIZED=0  # Set to '1' to enforce strict SSL checks, '0' to ignore self-signed certs

# Session Configuration (optional, if you're using sessions)
SESSION_SECRET="yourSessionSecretKey"  # Ensure this is a strong, unpredictable key
