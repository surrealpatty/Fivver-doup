# Database Configuration for Development
DB_USER=root                             # Database username for development
DB_PASSWORD=f0^:8t1#qaC7                 # Database password for development (ensure no extra spaces)
DB_NAME=fivver_doup                       # Development database name
DB_HOST=localhost                         # Database host for development
DB_PORT=3306                              # Default MySQL port
DB_DIALECT=mysql                          # Sequelize dialect for MySQL

# JWT Secret for Signing Tokens
JWT_SECRET=your_jwt_secret               # Replace with a secure, random secret key
JWT_EXPIRATION=1h                        # Token expiration time (example: 1 hour)

# Port Configuration
PORT=3000                                 # Port to run the app on
NODE_ENV=development                      # Environment: 'development', 'production', or 'test'

# Test Database Configuration (Separate DB for testing to avoid conflicts)
TEST_DB_USER=root                         # Test DB username
TEST_DB_PASSWORD=f0^:8t1#qaC7             # Test DB password (keep the format consistent)
TEST_DB_NAME=fivver_doup_test             # Test DB name, separate from development and production
TEST_DB_HOST=localhost                    # Test DB host (default is localhost)

# Optional Production Database Configuration (Uncomment and configure for production)
# PROD_DB_USER=root                         # Production DB username
# PROD_DB_PASSWORD=your_prod_db_password    # Production DB password
# PROD_DB_NAME=fivver_doup_prod             # Production DB name
# PROD_DB_HOST=your_prod_db_host           # Production DB host

# File Uploads or Caching Settings
UPLOAD_DIR=./uploads                      # Directory for storing uploaded files (ensure this path exists)
