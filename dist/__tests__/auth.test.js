"use strict";
const { authenticateToken } = require('middleware/authMiddleware');
describe('Authentication Middleware', () => {
    test('should authenticate a valid token', async () => {
        // Mock token and expected outcomes
    });
});
